struct Point {
    x: i32,
    y: i32,
}

struct Matrix {
    array: [i32],
    m: i32,
    n: i32,
}

fn main() {
    let point = Point { x: 4, y: 5 };
    let matrix = println!("x: {} y:{}", point.x, point.y);
}
